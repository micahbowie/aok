<%= form_with(url: :uid_acts, local: true) do |u_form| %>
   <%= u_form.fields_for :act do |form| %>
  <% if act.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(act.errors.count, "error") %> prohibited this act from being saved:</h2>

      <ul>
        <% act.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :unique_identifier %>
    <%= form.text_field :uid_reference %>
  </div>

  <div class="field">
    <%= form.label :giver %>
    <%= form.text_field :giver %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_area :message %>
  </div>

  <div class="field">
    <%= form.label :instagram_username %>
    <%= form.text_field :instagram_username %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
<% end %>
